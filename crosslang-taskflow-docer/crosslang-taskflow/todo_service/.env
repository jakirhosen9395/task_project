# =========================
# Database Configurations
# =========================

# --- Option 1: Local Docker Network (docker-compose / k8s) ---
# Works if MongoDB runs in a container named "mongo" in the same Docker network.
#MONGO_URI=mongodb://mongo:27017

# --- Option 2: Different Container Name ---
# Use if your MongoDB container has a custom name (e.g. "mongoddb").
MONGO_URI=mongodb://mongodb:27017

# --- Option 3: Self-hosted Mongo on VM or EC2 ---
# Use the private or public IP of the machine running MongoDB.
# Example for a local VM network:
#MONGO_URI=mongodb://192.168.122.10:27017
# Example for AWS EC2 in the same VPC:
#MONGO_URI=mongodb://10.0.2.15:27017
# Example for a server with DNS/hostname:
#MONGO_URI=mongodb://myserver.example.com:27017

# --- Option 4: Managed MongoDB Service (MongoDB Atlas, AWS DocumentDB, etc.) ---
# Use the connection string provided by the cloud provider.
# Example for MongoDB Atlas:
#MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/multi_lang_todo?retryWrites=true&w=majority


# =========================
# Application Configurations
# =========================

# Database name (can stay the same in all environments)
DB_NAME=multi_lang_todo

# JWT secret key — MUST be strong & random in production (32+ characters recommended)
JWT_SECRET=your_secret_key_here


# =========================
# Server Configurations
# =========================

# App port (can be overridden by container or cloud runtime)
PORT=8002
